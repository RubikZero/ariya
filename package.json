{
  "name": "ariya",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "db:migrate": "wrangler d1 migrations apply DB --remote",
    "db:migrate:dev": "wrangler d1 migrations apply DB --remote --config wrangler.dev.jsonc",
    "deploy": "npm run db:migrate && wrangler deploy",
    "deploy:dev": "npm run build:frontend && npm run db:migrate:dev && wrangler deploy --config wrangler.dev.jsonc",
    "dev": "wrangler dev --port 8787 --remote --config wrangler.dev.jsonc",
    "start": "wrangler dev --config wrangler.dev.jsonc --remote",
    "test": "vitest",
    "cf-typegen": "wrangler types",
    "build:frontend": "cd frontend && npm run build",
    "setup": "node scripts/setup.mjs",
    "setup:dev": "node scripts/setup.mjs dev",
    "gen-key": "node -e \"console.log(require('crypto').randomBytes(32).toString('hex'))\"",
    "predeploy": "npm run build:frontend"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.12.4",
    "@types/node": "^25.9.2",
    "typescript": "^5.5.2",
    "vitest": "~3.2.0",
    "wrangler": "^4.99.0"
  }
}
